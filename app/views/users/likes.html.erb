<% if logged_in? %>
  <h1>お気に入りMicroposts一覧</h1>
  <div class="row">
    <div class="col-xs-8">
      <%= render 'microposts/microposts', microposts: @like_microposts %>
    </div>
  </div>
<% end %>
